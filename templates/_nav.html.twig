<nav class="navbar navbar-expand-lg navbar-dark bg-secondary fixed-top">

    <!-- Navbar brand -->

    <img class="logo" alt="Logo" src="{{ asset('build/images/logoYvard.png') }}">
    <a class="navbar-brand" href="#">Wild Circus</a>
    <!-- Collapse button -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#basicExampleNav" aria-controls="basicExampleNav"
            aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible content -->
    <div class="collapse navbar-collapse" id="basicExampleNav">

        <!-- Links -->
        <ul class="navbar-nav ml-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home_index') }}">Acceuil
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('artist_index') }}">Artists</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Circus</a>
            </li>
            <li class="nav-item vertical-line">
                <a class="nav-link" href="#">booking</a>
            </li>
            <li class="nav-item vertical-line">
                <a class="nav-link" href="#">Touring</a>
            </li>
            <li class="nav-item vertical-line">
                <a class="nav-link" href="{{ path('contact_index') }}">Contact</a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item vertical-line">
                <a class="nav-link" href="{{ path('user_index') }}">Artist management</a>
            </li>
            {% endif %}
            {% if app.user %}
                <li class="nav-item vertical-line">
                    <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                </li>
            {% else %}
                <li class="nav-item vertical-line">
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                </li>
            {% endif %}
        </ul>
        <!-- Links -->
    </div>
    <!-- Collapsible content -->
</nav>


<!--/.Navbar-->